# Hooks (Template)

Place safe, scoped hooks here.

Recommended:
- PreToolUse checks for destructive commands
- PostToolUse formatting checks
- SessionStart reminders for workflow mode

Keep hooks deterministic and low-noise.
